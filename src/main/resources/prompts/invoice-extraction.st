Extract the following fields from this invoice document. Return ONLY a JSON object with these fields: vendor_name, invoice_number, issue_date (YYYY-MM-DD), due_date (YYYY-MM-DD), total_amount (as number), tax_amount (as number). If a field is not present, use null.

Document text:
{document_text}
